const DOMAIN = "learn.reboot01.com";
const authApiEndpoint = `https://${DOMAIN}/api/auth/signin`;
const GraphQLEndpoint = `https://${DOMAIN}/api/graphql-engine/v1/graphql`;

export { DOMAIN, authApiEndpoint, GraphQLEndpoint };